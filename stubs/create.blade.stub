@extends('layouts.app')

@section('content')
<h1>Create {{ Name }}</h1>
<form action="{{ route('{{ plural_route_name }}.store') }}" method="POST">
    @csrf
{{ form_fields }}
    <button type="submit" class="btn btn-primary">Save</button>
</form>
@endsection
